#include "main.h"

/**
 * _abs - outputs absolute value
 * @n: positive or negative number
 *
 * Description: null
 * Return: absolute value of n
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}
