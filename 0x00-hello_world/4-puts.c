#include <stdio.h>
#include <string.h>

/**
 * main - this is the main function
 *
 * Description: still the man function
 * Return: returns 0
 */
int main(void)
{	char str1[60];
	strcpy(str1, "\"Programming is like building a multilingual puzzle ");
	puts(str1);
	return (0);
}
