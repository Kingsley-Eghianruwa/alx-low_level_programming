#ifndef MAIN_H
#define MAIN_H

#include <stddef.h>
#include <stdio.h>
#include <string.h>
#include <math.h>

int _putchar(char);
int _strlen(char *s);
int _pow(int, int);
int _string2int(char *);
void _putint(int);
void _putstring(char *);
int getdigits(int);

#endif
